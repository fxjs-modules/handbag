import {
    setCompilerForVbox,
    wrapAsString
} from './vbox'

export const vboxUtils = {
    setCompilerForVbox,
    wrapAsString
}

import * as compilers from './compilers'
export { compilers }

import * as registers from './registers'
export { registers }